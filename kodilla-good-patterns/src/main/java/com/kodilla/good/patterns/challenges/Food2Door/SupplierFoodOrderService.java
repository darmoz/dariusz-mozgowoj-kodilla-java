package com.kodilla.good.patterns.challenges.Food2Door;

public class SupplierFoodOrderService implements FoodOrderService {

    public boolean order(Supplier supplier, String typeOfFood, double foodQuantity) {
        if (foodQuantity > 0) {
        }
            return true;
    }
}